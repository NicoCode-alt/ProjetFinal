{% extends 'base.html.twig' %}

{% block title %}Zikrit{% endblock %}

{% block body %}

    {% include 'partials/research.html.twig' %}
                   
    {% include 'partials/lateral_bar.html.twig' %}

    <h3 class="title_h3">Les catégories :</h3>

    <div class="container">
        <div class="row_categories flex-wrap">
            {% for category in categories %}

            <div class="col-2 categories_card">
                <div class="text">
                    <a href="#">{{ category.name }}</a>
                </div>
            </div>

            {% endfor %}
        </div> 
    </div>
    
    


{% endblock %}
